import random # import random module
x, y = random.uniform(6.5, 10.5), random.uniform(6.5, 10.5) # generate two random numbers between 6.5 and 10.5
print(x, y, round(x * y, 2)) # print the two random numbers and the product of them rounded to 2 decimal places